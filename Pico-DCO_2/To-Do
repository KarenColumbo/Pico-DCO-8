Output voice CVs on 8 GPIOs (CV_PIN[x]) //"Stole" the Range-pins that are inactive on this board. Still gotta work on the formula to output exponential CVs (maybe without DAC, but low pass filtering?) instead of crazy DCO stuff.
